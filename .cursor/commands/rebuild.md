# Rebuild Project

Run an incremental build of the morphizen-rocm project using the existing CMake cache.

## Steps

1. Execute the build command: `cmake --build C:/Develop/m/build/morphizen-rocm`
2. Check for build errors
3. Report success or failure with error details

Use the batch script at `tools/rebuild.bat` for direct execution.
