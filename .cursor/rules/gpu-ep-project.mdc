---
description: Project naming — hip-ep / GPU EP
alwaysApply: true
---

# GPU EP Project Context

The user is working on **hip-ep** (ROCm/onnx-hipdnn-ep), which they interchangeably call **GPU EP**.

- **Repo**: `https://github.com/ROCm/hip-ep.git`
- **Local folder** (when cloned): `hip-ep/`
- **Aliases**: "GPU EP", "hip-ep", "onnx-hipdnn-ep", "hipdnn EP" — treat these as the same project
- **Domain**: ROCm ONNX Execution Provider using hipDNN

When the user says "GPU EP", refer to hip-ep unless context clearly indicates otherwise.

**Build, test, and benchmark:** see `hip-ep/docs/quick_start.md` (official default flow; CI and release packages use the same steps).
