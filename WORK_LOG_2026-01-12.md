# morphizen-hipdnn 构建安装工作日志

**日期：** 2026年1月12日  
**操作员：** mingyue  
**目标：** 在 Windows 平台上构建和安装 morphizen-hipdnn 及其依赖组件

---

## 📋 目录

1. [环境信息](#环境信息)
2. [操作步骤](#操作步骤)
3. [遇到的问题及解决方案](#遇到的问题及解决方案)
4. [构建结果](#构建结果)
5. [环境配置总结](#环境配置总结)
6. [附录](#附录)

---

## 环境信息

### 硬件环境
- **GPU 型号：** AMD Radeon(TM) 8050S Graphics
- **GPU 架构：** gfx1151 (RDNA 3)
- **计算能力：** major: 11, minor: 5
- **显存：** 24.26 GB (集成显卡)
- **多处理器数量：** 16
- **时钟频率：** 2600 MHz

### 软件环境
- **操作系统：** Windows 10 (Build 26100)
- **CMake 版本：** 3.29
- **Shell：** PowerShell
- **编译器：** MSVC (Visual Studio)

### 目录结构
```
D:\Users\mingyue\hipdnn\workspace\
├── MorphiZen/                    # MorphiZen 主仓库
├── morphizen-hipdnn/             # hipDNN 集成项目
├── onnxruntime/                  # ONNX Runtime 源码
├── build/
│   ├── onnxruntime/              # ONNX Runtime 构建输出
│   └── morphizen-hipdnn/         # morphizen-hipdnn 构建输出
└── local/                        # 统一安装目录

D:\therock/                       # TheRock ROCm SDK
```

---

## 操作步骤

### 步骤 1：安装 AMD GPU 驱动程序 ✅

**操作内容：**
1. 从 AMD 官网下载驱动程序
   - 下载地址：https://www.amd.com/en/support/download/drivers.html
   - 根据 GPU 型号选择合适的驱动

2. 安装驱动程序并重启系统

3. 验证安装
   - 打开 **设备管理器** (Device Manager)
   - 导航到 **显示适配器** (Display adapters)
   - 确认显示：`AMD Radeon(TM) 8050S Graphics`

**结果：** ✅ 驱动安装成功

---

### 步骤 2：安装 ROCm SDK (TheRock) ✅

**操作内容：**

**2.1 下载 TheRock SDK**
- 来源：https://therock-nightly-tarball.s3.amazonaws.com/index.html
- 需要根据 GPU 架构选择对应版本（gfx115X 系列）

**2.2 解压到目标目录**
```powershell
# 解压 TheRock tarball 到 D:\therock
tar -xzf therock-dist-windows-*.tar.gz -C D:\
```

**安装位置：** `D:\therock`

**结果：** ✅ TheRock SDK 安装成功

---

### 步骤 3：获取 GPU 架构信息 ✅

**执行命令：**
```powershell
D:\therock\bin\hipInfo.exe
```

**关键输出信息：**
```
Name:                             AMD Radeon(TM) 8050S Graphics
gcnArchName:                      gfx1151
major:                            11
minor:                            5
totalGlobalMem:                   24.26 GB
memInfo.free:                     24.10 GB (99%)
multiProcessorCount:              16
clockRate:                        2600 Mhz
isIntegrated:                     1
```

**关键参数：**
- **gcnArchName: gfx1151** （后续编译和配置的关键参数）
- GFX 系列：gfx115X (RDNA 3 架构)

**结果：** ✅ 成功获取 GPU 架构信息

---

### 步骤 4：切换工作目录

**执行命令：**
```powershell
cd D:\Users\mingyue\hipdnn\workspace
```

---

### 步骤 5：编译安装 ONNX Runtime ✅

**操作内容：**

**5.1 克隆 ONNX Runtime 仓库**
```powershell
git clone https://github.com/Microsoft/onnxruntime.git
cd onnxruntime
```

**5.2 配置并构建 ONNX Runtime**
```powershell
./build.bat --use_vitisai `
  --config Debug `
  --build_shared_lib `
  --parallel `
  --compile_no_warning_as_error `
  --skip_submodule_sync `
  --build_dir ../build/onnxruntime `
  --skip_tests `
  --cmake_extra_defines CMAKE_INSTALL_PREFIX=D:\Users\mingyue\hipdnn\workspace\local
```

**构建参数说明：**
- `--use_vitisai` - 启用 Vitis AI 支持（用于自定义算子）
- `--config Debug` - Debug 模式编译
- `--build_shared_lib` - 构建共享库
- `--parallel` - 并行编译
- `--compile_no_warning_as_error` - 警告不作为错误处理
- `--skip_submodule_sync` - 跳过子模块同步
- `--build_dir ../build/onnxruntime` - 构建输出目录
- `--skip_tests` - 跳过测试
- `CMAKE_INSTALL_PREFIX` - 安装路径

**5.3 安装到指定目录**
```powershell
cmake --build ../build/onnxruntime/Debug/ --target install
```

**安装位置：** `D:\Users\mingyue\hipdnn\workspace\local`

**结果：** ✅ ONNX Runtime 编译安装成功

---

### 步骤 6：克隆 MorphiZen 相关项目 ✅

**操作内容：**

**6.1 返回工作区目录**
```powershell
cd D:\Users\mingyue\hipdnn\workspace
```

**6.2 克隆 MorphiZen 仓库**
```powershell
git clone git@gitenterprise.xilinx.com:VitisAI/MorphiZen.git --recursive
```
- 使用 `--recursive` 参数克隆所有子模块

**6.3 克隆 morphizen-hipdnn 仓库**
```powershell
git clone git@gitenterprise.xilinx.com:VitisAI/morphizen-hipdnn.git
```

**结果：** ✅ 仓库克隆成功

---

### 步骤 7：配置 morphizen-hipdnn 项目

**7.1 初始配置命令**
```powershell
cd morphizen-hipdnn
cmake -DBUILD_SHARED_LIBS=OFF `
  -B ../build/morphizen-hipdnn `
  -S . `
  -DCMAKE_INSTALL_PREFIX=D:\Users\mingyue\hipdnn\workspace\local `
  -DTHEROCK_DIST="D:\therock"
```

**CMake 配置参数：**
- `-DBUILD_SHARED_LIBS=OFF` - 构建静态库
- `-B ../build/morphizen-hipdnn` - 构建目录
- `-S .` - 源代码目录
- `-DCMAKE_INSTALL_PREFIX` - 安装前缀（与 ONNX Runtime 相同）
- `-DTHEROCK_DIST="D:\therock"` - 指定 TheRock HIP SDK 路径

**初始结果：** ❌ 遇到配置错误（见下一节）

---

## 遇到的问题及解决方案

### 问题 1：找不到 nlohmann_json 包 ❌

**错误信息：**
```
CMake Error: Could not find a package configuration file provided by "nlohmann_json"
```

**错误原因：**
TheRock SDK 中的 nlohmann_json CMake 配置文件包含有问题的 `INTERFACE_SOURCES` 属性。

**解决方案：** ✅

**文件路径：** `D:\therock\share\cmake\nlohmann_json\nlohmann_jsonTargets.cmake`

**修改内容：**
1. 打开文件并查找 `set_target_properties(nlohmann_json::nlohmann_json ...)`
2. **删除 `INTERFACE_SOURCES` 行**（如果存在）

**修改示例：**
```cmake
# 修改前：
set_target_properties(nlohmann_json::nlohmann_json PROPERTIES
  INTERFACE_INCLUDE_DIRECTORIES "${_IMPORT_PREFIX}/include"
  INTERFACE_SOURCES "B:/build/some/missing/file.cpp"  # ← 删除此行
)

# 修改后：
set_target_properties(nlohmann_json::nlohmann_json PROPERTIES
  INTERFACE_INCLUDE_DIRECTORIES "${_IMPORT_PREFIX}/include"
)
```

**参考文档：** `doc/HIPDNNEP_INSTALLATION.md` 第 128-136 行

---

### 问题 2：hipdnn_frontend 包含不存在的路径 ❌

**错误信息：**
```
CMake Error: Imported target "hipdnn_frontend" includes non-existent path
  "B:/build/third-party/flatbuffers/dist/include"
```

**错误原因：**
TheRock SDK 的 CMake 配置文件包含构建时的硬编码绝对路径（`B:/build/...`），在当前机器上不存在。

**解决方案：** ✅

**需要修改的文件：**

#### 文件 1：`D:\therock\lib\cmake\hipdnn_frontend\hipdnn_frontendTargets.cmake`

**修改内容：**
1. 查找 `set_target_properties(hipdnn_frontend ...)`
2. 找到 `INTERFACE_INCLUDE_DIRECTORIES` 行
3. **删除所有以 `B:/build/` 开头的绝对路径**
4. **只保留 `${_IMPORT_PREFIX}/include`**

**修改示例：**
```cmake
# 修改前：
set_target_properties(hipdnn_frontend PROPERTIES
  INTERFACE_COMPILE_FEATURES "cxx_std_17"
  INTERFACE_INCLUDE_DIRECTORIES "B:/build/third-party/flatbuffers/dist/include;B:/build/other/path;${_IMPORT_PREFIX}/include"
  INTERFACE_LINK_LIBRARIES "hip::host"
)

# 修改后：
set_target_properties(hipdnn_frontend PROPERTIES
  INTERFACE_COMPILE_FEATURES "cxx_std_17"
  INTERFACE_INCLUDE_DIRECTORIES "${_IMPORT_PREFIX}/include"
  INTERFACE_LINK_LIBRARIES "hip::host"
)
```

#### 文件 2：`D:\therock\lib\cmake\hipdnn_data_sdk\hipdnn_data_sdkTargets.cmake`

**修改内容：**
同样删除所有硬编码的绝对路径（`B:/build/...`），只保留 `${_IMPORT_PREFIX}/include`

**修改示例：**
```cmake
# 修改前：
INTERFACE_INCLUDE_DIRECTORIES "B:/build/third-party/llvm-project/install/include;B:/build/third-party/json/install/include;${_IMPORT_PREFIX}/include"

# 修改后：
INTERFACE_INCLUDE_DIRECTORIES "${_IMPORT_PREFIX}/include"
```

**参考文档：** `doc/HIPDNNEP_INSTALLATION.md` 第 102-137 行

---

### 问题 3：找不到 GSL 头文件 ❌

**错误信息：**
```
error C1083: Cannot open include file: 'gsl/gsl': No such file or directory
位置：D:\Users\mingyue\hipdnn\workspace\onnxruntime\onnxruntime\core\providers\vitisai\include\vaip\vaip_gsl.h(8,10)
```

**错误原因：**
- 缺少 **Microsoft GSL** (Guidelines Support Library)
- ONNX Runtime 的 VitisAI provider 头文件依赖此库
- 编译 MorphiZen 的 `mem_binary` 测试时触发

**解决方案：** ✅

修改 MorphiZen 项目的 CMakeLists.txt，显式添加 Microsoft GSL 依赖。

#### 文件 1：`MorphiZen/mem_binary/CMakeLists.txt`

**修改内容（第 40 行）：**
```diff
target_link_libraries(
  ${LIB_NAME}
  PRIVATE morphizen-utils
- PUBLIC ZLIB::ZLIB glog::glog)
+ PUBLIC ZLIB::ZLIB glog::glog Microsoft.GSL::GSL)
```

#### 文件 2：`MorphiZen/mem_binary/test/CMakeLists.txt`

**修改内容（第 36 行）：**
```diff
target_link_libraries(${TEST_EXE_NAME} PRIVATE GTest::gtest morphizen-utils
-                                               ZLIB::ZLIB glog::glog)
+                                               ZLIB::ZLIB glog::glog Microsoft.GSL::GSL)
```

**修改说明：**
- 在 `mem_binary` 库中添加了 `Microsoft.GSL::GSL` 依赖
- 在 `test_mem_binary` 测试程序中添加了 `Microsoft.GSL::GSL` 依赖
- 这样可以正确链接 Microsoft GSL 库，解决头文件找不到的问题

---

### 步骤 8：成功构建并安装 ✅

**执行命令：**
```powershell
cmake --build ../build/morphizen-hipdnn --config Debug --target install
```

**构建参数：**
- `--build ../build/morphizen-hipdnn` - 构建目录
- `--config Debug` - Debug 配置模式
- `--target install` - 执行安装目标

**结果：** ✅ 构建成功！

---

## 构建结果

### 安装配置
- **构建配置：** Debug
- **构建目录：** `D:\Users\mingyue\hipdnn\workspace\build\morphizen-hipdnn`
- **安装目录：** `D:\Users\mingyue\hipdnn\workspace\local`

### 安装的文件

#### 可执行文件 (bin/)
```
D:\Users\mingyue\hipdnn\workspace\local\bin\
├── vaip_config.json                    # VAIP 配置文件
├── morphizen-graph-opt.exe             # 图优化工具
├── morphizen-tar.exe                   # TAR 打包工具
├── morphizen-compile-pattern.exe       # 模式编译工具
├── morphizen-onnx-grep.exe             # ONNX 搜索工具
├── hello_plugin_dll.exe                # 插件示例
└── hello_runner.exe                    # 运行器示例
```

#### 库文件 (lib/)
```
D:\Users\mingyue\hipdnn\workspace\local\lib\
├── onnxruntime_vitisai_ep.lib          # ONNX Runtime VitisAI EP (静态库)
├── onnxruntime_vitisai_ep.dll          # ONNX Runtime VitisAI EP (动态库)
├── hello_plugin_dll.lib                # 插件库
└── [其他静态库和protobuf文件]
```

#### 组件列表
构建了以下组件：
- ✅ `hipdnn_proto` - Protobuf 定义
- ✅ `morphizen-level1-pass-hipdnn` - Level-1 Pass
- ✅ `morphizen-custom-op-hipdnn` - Custom Operator
- ✅ `morphizen-unit-tests` - 单元测试
- ✅ 各种工具和示例程序

---

## 环境配置总结

### 关键路径配置

| 组件 | 路径/值 |
|------|---------|
| **GPU 型号** | AMD Radeon(TM) 8050S Graphics |
| **GPU 架构** | gfx1151 |
| **GFX 系列** | gfx115X (RDNA 3) |
| **TheRock SDK** | D:\therock |
| **工作目录** | D:\Users\mingyue\hipdnn\workspace |
| **安装目录** | D:\Users\mingyue\hipdnn\workspace\local |
| **构建目录** | D:\Users\mingyue\hipdnn\workspace\build |
| **ONNX Runtime 源码** | D:\Users\mingyue\hipdnn\workspace\onnxruntime |
| **MorphiZen 源码** | D:\Users\mingyue\hipdnn\workspace\MorphiZen |
| **morphizen-hipdnn 源码** | D:\Users\mingyue\hipdnn\workspace\morphizen-hipdnn |

### CMake 配置参数（完整）

```powershell
cmake -DBUILD_SHARED_LIBS=OFF `
  -B ../build/morphizen-hipdnn `
  -S . `
  -DCMAKE_INSTALL_PREFIX=D:\Users\mingyue\hipdnn\workspace\local `
  -DTHEROCK_DIST="D:\therock"
```

### 环境变量（推荐设置）

```powershell
# TheRock SDK
$env:THEROCK_DIST = "D:\therock"
$env:HIP_PLATFORM = "amd"
$env:PATH = "D:\therock\bin;" + $env:PATH

# 安装目录
$env:PATH = "D:\Users\mingyue\hipdnn\workspace\local\bin;" + $env:PATH
```

---

## 附录

### A. 已完成步骤清单

- [x] 1. 安装 AMD GPU 驱动程序
- [x] 2. 安装 ROCm SDK (TheRock)
- [x] 3. 获取 GPU 架构信息 (gfx1151)
- [x] 4. 切换工作目录
- [x] 5. 编译安装 ONNX Runtime (with VitisAI)
- [x] 6. 克隆 MorphiZen 和 morphizen-hipdnn 仓库
- [x] 7. 修复 TheRock SDK CMake 配置文件
  - [x] nlohmann_json 配置
  - [x] hipdnn_frontend 配置
  - [x] hipdnn_data_sdk 配置
- [x] 8. 修复 GSL 依赖问题
- [x] 9. 成功构建并安装 morphizen-hipdnn

### B. 修改的文件清单

#### TheRock SDK 配置文件修复
1. `D:\therock\share\cmake\nlohmann_json\nlohmann_jsonTargets.cmake`
   - 删除 `INTERFACE_SOURCES` 行

2. `D:\therock\lib\cmake\hipdnn_frontend\hipdnn_frontendTargets.cmake`
   - 删除硬编码绝对路径（`B:/build/...`）

3. `D:\therock\lib\cmake\hipdnn_data_sdk\hipdnn_data_sdkTargets.cmake`
   - 删除硬编码绝对路径（`B:/build/...`）

#### MorphiZen 源码修改
1. `MorphiZen/mem_binary/CMakeLists.txt`
   - 添加 `Microsoft.GSL::GSL` 依赖

2. `MorphiZen/mem_binary/test/CMakeLists.txt`
   - 添加 `Microsoft.GSL::GSL` 依赖

### C. 参考文档

项目中的相关文档：
- `doc/HIPDNN_WINDOWS_SETUP.md` - Windows 平台 hipDNN 设置指南
- `doc/HIPDNNEP_INSTALLATION.md` - hipDNNEP 安装指南
- `doc/BUILD_FIXES.md` - 构建修复说明
- `doc/IMPLEMENTATION_GUIDE.md` - 实现指南
- `doc/CONV_IMPLEMENTATION.md` - 卷积实现说明

### D. 下一步建议

1. **运行测试验证**
   ```powershell
   cd D:\Users\mingyue\hipdnn\workspace\build\morphizen-hipdnn
   ctest -C Debug
   ```

2. **配置永久环境变量**
   - 将 TheRock SDK 路径添加到系统 PATH
   - 设置 HIP_PLATFORM=amd
   - 设置 THEROCK_DIST 环境变量

3. **运行示例程序**
   ```powershell
   D:\Users\mingyue\hipdnn\workspace\local\bin\hello_runner.exe
   ```

4. **集成到其他项目**
   - 使用安装的 ONNX Runtime VitisAI EP
   - 开发自定义算子

### E. 故障排查提示

如果遇到运行时错误：

1. **检查 DLL 依赖**
   ```powershell
   dumpbin /dependents D:\Users\mingyue\hipdnn\workspace\local\lib\onnxruntime_vitisai_ep.dll
   ```

2. **验证 HIP 运行时**
   ```powershell
   D:\therock\bin\hipconfig.exe --version
   ```

3. **检查 GPU 可见性**
   ```powershell
   D:\therock\bin\hipInfo.exe
   ```

---

## 总结

本次构建成功完成了以下主要任务：
1. ✅ 配置了 AMD GPU 驱动和 ROCm SDK (TheRock)
2. ✅ 编译安装了 ONNX Runtime (with VitisAI support)
3. ✅ 解决了 TheRock SDK 配置文件的路径问题
4. ✅ 修复了 GSL 依赖问题
5. ✅ 成功构建了 morphizen-hipdnn 及所有组件

所有组件已安装到 `D:\Users\mingyue\hipdnn\workspace\local`，可以开始使用或继续开发。

---

**文档生成时间：** 2026年1月12日  
**状态：** ✅ 构建成功完成
