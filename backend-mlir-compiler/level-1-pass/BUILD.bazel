load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "morphizen-level1-pass-mlir-compiler",
    srcs = ["src/pass_main.cpp", "src/MlirCompiler.cpp"],
    hdrs = ["src/MlirCompiler.h"],
    copts = select({
        "@platforms//os:windows": ["/std:c++17", "/W3"],
        "//conditions:default": ["-std=c++17", "-fPIC"],
    }),
    defines = ["OUTPUT_NAME=\\\"morphizen-level1-pass-mlir-compiler\\\""],
    includes = ["src"],
    alwayslink = True,  # CRITICAL: equivalent to WHOLE_ARCHIVE
    linkstatic = True,
    visibility = ["//visibility:public"],
    deps = [
        "//backend-mlir-compiler/common:common_headers",
        "//backend-mlir-compiler/proto:metadata_headers",
        "//include:hip_headers",
        "//include:foundation_headers",
        "@glog//:glog",
        "@morphizen//morphizen-core:morphizen-core-static",
    ],
)
