<!--
Copyright (C) 2023 - 2025 Advanced Micro Devices, Inc. All rights reserved.
Licensed under the MIT License.
-->
# MorphiZen EP Integration

**Date:** 2026-03-04
**Document Type:** Design
**Review Status:** Self-Reviewed
**Related:** CONSTANT-HANDLING-DESIGN.md, RUNTIME-ARCHITECTURE.md, ARCHITECTURE.md

---

## Table of Contents

- [Overview](#overview)
- [Contract 2: morphizen-ep.dll → hip-compiler.dll](#contract-2-morphizen-epdll--hip-compilerdll)
- [Contract 3: morphizen-ep.dll → model.dll](#contract-3-morphizen-epdll--modeldll)
- [FileSystem: The Common Thread](#filesystem-the-common-thread)
- [Decoupling Benefits](#decoupling-benefits)
- [Related Documents](#related-documents)

---

## Overview

The system spans four DLLs across two phases:

| Phase | DLLs involved |
|-------|---------------|
| Compile time (session creation) | `onnxruntime.dll` → `morphizen-ep.dll` → `hip-compiler.dll` |
| Runtime (inference) | `onnxruntime.dll` → `morphizen-ep.dll` → `model.dll` |

`hip-compiler.dll` is **not loaded at inference time**. `model.dll` is **not
loaded at compile time**. The EP tar cache — accessed through the `FileSystem`
abstraction — is the only persistent link between the two phases.

`morphizen-ep.dll` integrates with `onnxruntime.dll` via the standard
[ORT Plugin EP v2 API](https://onnxruntime.ai/docs/execution-providers/plugin-ep-libraries.html).
The two contracts specific to this project are described below.

**Compile time (session creation):**

```
  ╔══════════════════════╗
  ║   onnxruntime.dll    ║
  ║   GetCapability()    ║
  ║   Compile()          ║
  ╚══════════╤═══════════╝
             │ Compile callback
             ▼
  ╔═════════════════════════════════╗
  ║         morphizen-ep.dll        ║
  ║                                 ║
  ║  ┌─────────────────────────┐    ║
  ║  │ level-1 pass (HipDnnEP) │    ║
  ║  │ OrtGraph* → IRConverter │    ║
  ║  │        → input_mlir     │    ║
  ║  │ fs = get_file_system()  │    ║
  ║  └─────────────────────────┘    ║
  ╚═════════════════╤═══════════════╝
                    │ hip_compile_with_fs(input_mlir, .., fs)
                    ▼
  ╔═════════════════════════════════╗
  ║       hip-compiler.dll         ║
  ║  fs->create_writer(name)        ║
  ║  → FileWriter → fwrite()        ║
  ╚══════════════════╤══════════════╝
                     │
               EP tar cache
```

**Runtime (inference) — hip-compiler.dll NOT loaded:**

```
  ╔══════════════════════╗
  ║   onnxruntime.dll    ║
  ║   CreateState()      ║
  ║   Compute()          ║
  ║   ReleaseState()     ║
  ╚══════════╤═══════════╝
             │ CreateState / Compute / ReleaseState callbacks
             ▼
  ╔═════════════════════════════════╗
  ║         morphizen-ep.dll        ║
  ║                                 ║
  ║  ┌─────────────────────────┐    ║
  ║  │   MyCustomOp (HipDnnEP) │    ║
  ║  │ fs = get_file_system()  │    ║
  ║  └─────────────────────────┘    ║
  ╚═════════════════╤═══════════════╝
                    │ inference_init(fs)
                    │ inference_compute(state, inputs, outputs)
                    │ inference_cleanup(state)
                    ▼
  ╔═════════════════════════════════╗
  ║           model.dll             ║
  ║  fs->create_reader(name)        ║  ← reads from EP tar cache
  ║  → FileReader                   ║     (written at compile time)
  ║     fread() / mmap()            ║
  ╚═════════════════════════════════╝
```

---

## Contract 2: morphizen-ep.dll → hip-compiler.dll

During `GetCapability`, the level-1 pass first converts the `OrtGraph` object
to MLIR IR bytes:

```
OrtGraph* → IRConverter::to_onnx_model()  →  MLIR bytes (input_mlir)
```

> **Note:** `to_onnx_model` is named after the first of two IR backends that
> morphizen-ep supports. The HipDnnEP level-1 pass uses the MLIR backend. See
> [footnote](#footnote-irconverter) for background.

It then calls into `hip-compiler.dll` via a single C function:

```c
// include/hip-compiler/compiler_api.h
CompilerErrorCode hip_compile_with_fs(
    const void*    input_mlir,   // MLIR IR bytes (from IRConverter)
    size_t         input_size,
    const char*    output_path,  // logical name for the compiled artifact in the cache
    const char*    options_json, // compilation options
    CompilerError* error,
    void*          fs);          // morphizen::FileSystem* — storage backend
```

`morphizen-ep.dll` has no knowledge of how `hip-compiler.dll` compiles the
model. Any compiler that implements this API can replace `hip-compiler.dll`
without changes to the EP.

---

## Contract 3: morphizen-ep.dll → model.dll

`model.dll` is generated by `hip-compiler.dll` and exports three C functions:

```c
int inference_init(void** out_state, void* fs);
int inference_compute(void* state, span_t* inputs, span_t* outputs);
int inference_cleanup(void* state);
```

`inference_init` receives the same `FileSystem*` that the level-1 pass used
during compilation, giving `model.dll` access to the EP cache.
`inference_compute` is called once per inference run. `inference_cleanup` is
called at session destruction.

`morphizen-ep.dll` has no knowledge of what `model.dll` uses internally. The
compiler has full freedom in what it generates inside `model.dll` — target
hardware, libraries, or compute strategy — without any modification to the EP.
For the full C interface specification, see
[mlir/INTERFACE-DESIGN.md](mlir/INTERFACE-DESIGN.md).

---

## FileSystem: The Common Thread

`morphizen::FileSystem` (defined in `morphizen-foundation/file_io.hpp`) is the
only data channel that crosses both DLL boundaries:

```
PassContext::get_file_system()
        │
        │  fs passed to hip-compiler.dll at compile time
        │  fs passed to model.dll at runtime
        ▼
  ┌─────────────────────────┐
  │      EP tar cache        │  ← storage backend, opaque to compiler and model
  │  (or DiskFileSystem for  │
  │   CLI / standalone use)  │
  └─────────────────────────┘
```

| Interface    | Key methods                   | Used by             |
|--------------|-------------------------------|---------------------|
| `FileSystem` | `create_writer(path)`         | `hip-compiler.dll` |
| `FileSystem` | `create_reader(path)`         | `model.dll`         |
| `FileWriter` | `fwrite(buf, size)`           | `hip-compiler.dll` |
| `FileReader` | `fread(buf, size)`, `mmap()`  | `model.dll`         |

---

## Decoupling Benefits

The three contracts are deliberately minimal C APIs. This gives each project
the freedom to evolve independently:

- **Compiler project** (`hip-compiler.dll`) and **EP project**
  (`morphizen-ep.dll`) are coupled only through `hip_compile_with_fs()`.
  The compiler implementation — passes, optimisations, code generation
  strategy — can change without any modification to the EP.

- **Model runtime** (`model.dll`) and the EP are coupled only through the
  three C function signatures and the `FileSystem*` channel. The compiler has
  full freedom in what it generates inside `model.dll` — target hardware,
  libraries, or compute strategy — without any modification to the EP.

- **Storage backend** (`FileSystem`) is opaque to both `hip-compiler.dll`
  and `model.dll`. The EP decides where artifacts live — in-memory tar cache,
  disk, or a network store — without either project needing to know.

---

## Related Documents

- [CONSTANT-HANDLING-DESIGN.md](CONSTANT-HANDLING-DESIGN.md) — constants file format, write order, and runtime upload
- [RUNTIME-ARCHITECTURE.md](RUNTIME-ARCHITECTURE.md) — opaque `RuntimeState`, IR merging, zero-cost abstraction
- [ARCHITECTURE.md](ARCHITECTURE.md) — system-wide design decisions
- [mlir/INTERFACE-DESIGN.md](mlir/INTERFACE-DESIGN.md) — C interface specification for `model.dll`

---

## Footnote: IRConverter

`IRConverter::to_onnx_model` is named after the first backend implementation.
morphizen-ep supports two interchangeable IR backends, both implementing the
`MORPHIZEN_ORT_API` interface:

- `onnx-ir-imp` — represents the graph as ONNX IR (the original backend)
- `mlir-imp` — represents the graph as MLIR (used by HipDnnEP)

Both expose the same `to_onnx_model` entry point so that the level-1 pass
remains backend-agnostic. The active backend is selected at runtime via the
`MORPHIZEN_ORT_BRIDGE_BACKEND` environment variable or compile-time default.
