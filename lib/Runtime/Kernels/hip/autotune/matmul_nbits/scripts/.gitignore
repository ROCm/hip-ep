# raw autotune sweep logs (large, machine-specific; inputs to update_lut.py
# build). The reviewable source of truth is lut/<arch>.json; the shipped
# artifact is lut/<arch>.fb (embedded into the DLL at build time).
data/
