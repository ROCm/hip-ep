[pytest]
testpaths = tests
addopts = -v --tb=short -m "not slow"
markers =
    slow: heavy single-op tests (multi-GB working sets, multi-minute CPU reference). Run with `pytest -m slow` or `pytest -m "slow or not slow"`.
